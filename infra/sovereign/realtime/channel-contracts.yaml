apiVersion: centrifugo.billyronks/v1
kind: ChannelContractSet
metadata:
  name: erp-assistant-channels
  generatedAt: "2026-02-28"
spec:
  channelPattern: tenant:{tenant_id}:{resource}
  authClaims:
    - tenant_id
    - role
    - scope
  channels:
    - name: assistant:runs
      subscribeRole: tenant_user
      publishRole: service_account
    - name: assistant:approvals
      subscribeRole: tenant_user
      publishRole: service_account
    - name: assistant:events
      subscribeRole: tenant_user
      publishRole: service_account
